#include<stdio.h>
int main(){
    int t;
    scanf("%d",&t);
    p = isPrime(t);
    printf(p);
}
int isPrime(int x){
    int r,c=0,d = n;
    for(int i = 1;i <= x;i++){
      if(x % n == 0)
      c = c + 1;  
    }
    if (c == 2)
    return d;
}